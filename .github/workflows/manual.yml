name: Manual workflow
on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Send greeting
        run: echo "Hello There"
      - name: Reusable
        uses: ceharsh24/gha-try/blob/main/.github/workflows/reusable-workflow.yml@master
    
